'use strict';

// Obiekt math

console.log( Math.PI );
console.log( Math.cos(0) );

// pole koła

console.log(Math.PI * Math.pow(4, 2));

// wartosc random jest od 0 do 1, ale 1 nie wygeneruje
var rand = Math.random();
console.log(rand);